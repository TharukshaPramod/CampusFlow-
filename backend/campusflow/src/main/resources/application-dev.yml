spring:
  profiles: dev
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/campusflow_dev}
    username: ${DB_USER:campusflow}
    password: ${DB_PASSWORD:campusflow}
  jpa:
    show-sql: true

logging:
  level:
    root: DEBUG
    '[org.hibernate.SQL]': DEBUG
